name: Keepalive

on:
  schedule:
    - cron: "*/30 * * * *"   # every 30 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl homepage
        run: |
          curl -sSfL https://otaku-manga.site/ > /dev/null
